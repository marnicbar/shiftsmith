# Tests provision the admin password explicitly, so the seeded account is treated
# as operator-chosen and is not gated behind a forced password change. This keeps
# the REST round-trip IT able to log in as admin/shiftsmith and hit protected
# endpoints without first rotating the credential.
shiftsmith.admin.password=shiftsmith
